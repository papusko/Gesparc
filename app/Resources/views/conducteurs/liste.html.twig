<div class="row color-bg">


    <div class="col-md-12 about-text">

        <table class="table table-striped">
            <tbody>
            <tr>
                <td colspan="1">
                    
                        <fieldset>
<div class="container">
        <div class="row">
            <div class="panel panel-primary filterable">
                <div class="panel-heading">
                    <h3 class="panel-title">Liste  des responsable de vehicule </h3>
                    <div class="pull-right">
                        <button class="btn btn-default btn-xs btn-filter"><span
                                class="glyphicon glyphicon-filter"></span> Filter
                        </button>
                    </div>
                </div>
                <table class="table">
                    <thead>
                    <tr class="filters">
						<th><input type="text" class="form-control" placeholder="Nom du reponsable " disabled></th>
						<th><input type="text" class="form-control" placeholder="Prénom du reponsable " disabled></th>
						<th><input type="text" class="form-control" placeholder="Adresse du responsable" disabled></th>
						<th><input type="text" class="form-control" placeholder="Téléphone du responsable" disabled></th>
                    </tr>
                    </thead>
                    <tbody>
					{% for c in conducteurs %}
                    <tr>
                        <td>{{ c.nom }}</td>
						<td>{{ c.prenom }}</td>
						<td>{{ c.adresse}}</td>}}>
						<td>{{ c.telephone}}</td>}}>
                    </tr>
					{% endfor %}
                    </tbody>
                </table>
				</div>
				
              
            </div>
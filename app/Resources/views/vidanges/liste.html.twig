<div class="row color-bg">


    <div class="col-md-12 about-text">

        <table class="table table-striped">
            <tbody>
            <tr>
                <td colspan="1">
                    
                        <fieldset>
<div class="container">
        <div class="row">
            <div class="panel panel-primary filterable">
                <div class="panel-heading">
                    <h3 class="panel-title">Liste des vidanges par voitures</h3>
                    <div class="pull-right">
                        <button class="btn btn-default btn-xs btn-filter"><span
                                class="glyphicon glyphicon-filter"></span> Filter
                        </button>
                    </div>
                </div>
                <table class="table">
                    <thead>
                    <tr class="filters">
						<th><input type="text" class="form-control" placeholder="Date de vidanges " disabled></th>
						<th><input type="text" class="form-control" placeholder="Kilometrage /debut " disabled></th>
						<th><input type="text" class="form-control" placeholder="Kilometrage /Fin " disabled></th>
                        <th><input type="text" class="form-control" placeholder="Voiture concernés " disabled></th>
                    </tr>
                    </thead>
                    <tbody>
					{% for v in vidanges %}
                    <tr>
                        <td>{{ v.dateDeVidange()|date('d/m/y') }}</td>
						<td>{{ v.KiloDebut }}</td>
						<td>{{ v.KiloFin }}</td>
						<td>{{ v.vehicules.immatriculation}}</td>}}>
						
						

                    </tr>
					{% endfor %}
                    </tbody>
                </table>
				</div>
				
              
            </div>
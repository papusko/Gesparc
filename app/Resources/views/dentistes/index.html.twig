{% extends 'base.html.twig' %}

{% block title %}Hello DentistesController!{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Ajout d'un bon de commande  ✅</h1>
    {{ form_start(form) }}
		    
		    	<div >{{ form_row(form.prenom) }}</div>
		    	<div >{{ form_row(form.nom) }}</div>
		    	<div >{{ form_row(form.adresse) }}</div>	
		    	<div >{{ form_row(form.telephone) }}</div>
		    	<div >{{ form_row(form.cabinet) }}</div>
    {{ form_widget(form) }}
    <button class="btn btn-primary"> valider</button>
    {{ form_end(form) }}
</div>
{% endblock %}
